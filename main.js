/* const HEIGHT_SCALER = 1.5;

changeProjectHeight = () => {
    let projectCollection = document.getElementsByClassName("project");

    for (let project of projectCollection) {
        let projectWidth = project.offsetWidth;
        project.style.height = `${projectWidth * HEIGHT_SCALER}px`;
    }
}

window.onresize = changeProjectHeight; */